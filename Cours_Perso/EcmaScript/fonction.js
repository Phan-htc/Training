/*==============================================================================================================================*/
//                                                         LES FONCTIONS
/*==============================================================================================================================*/
/*ne fonction est une procédure JavaScript, un ensemble d'instructions effectuant une tâche ou calculant une valeur.

// Une fonction retourne toujours quelque chose
// Il existe de nombreux fonctions intégrer a javascript, mais on doit quand meme faire nos propres fonction.
*/

function carré(nombre) {            // on déclare la fonction avec le mot clé function puis son nom et avec les (parametres)
    return nombre * nombre;         // instruction de la fonction
  };

// on peux appeler ses fonctions ailleurs dans le code de la manière suivante
var x = carré(4); // le résultat qui va retourner est 16

